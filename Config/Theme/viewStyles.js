import colors from './colors';

const viewStyles = {
  'te.views.Messages': {
    messageWrapper: {
      right: {
        backgroundColor: colors.turquoise,
      },
    },
  },
};

export default viewStyles;
