import React from 'react';
export interface PubNubConsumerProps {
    children: (client: object) => React.ReactChild | null;
}
export declare const PubNubConsumer: React.FC<PubNubConsumerProps>;
